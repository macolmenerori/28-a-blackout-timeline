import { describe, expect, it } from 'vitest';

import { App } from './App';

import { mockTimelineDataEs } from '@/test/mocks/timelineData';
import { mockUseSWR } from '@/test/setup';
import { render, screen } from '@/test/utils/test-utils';

describe('App', () => {
  it.todo('theme switch');
});
